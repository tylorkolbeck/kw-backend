{
  "routes": [
    {
      "method": "POST",
      "path": "/newsletter",
      "handler": "newsletterSubscriber.index",
      "config": {
        "policies": ["plugins::users-permissions.ratelimit"]
      }
    }
  ]
}
